import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.migestortareas.app',
  appName: 'MiGestorTareas',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
